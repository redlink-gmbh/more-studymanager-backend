# graphify
- **graphify** (`.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
When the user types `/graphify`, invoke the Skill tool with `skill: "graphify"` before doing anything else.

# architecture
- **architecture.md** (`architecture.md`) - project module layout and layering. Read before making structural or cross-module changes.

# context7
- **context7** - up-to-date library/framework docs. Use for any setup, API, or version-specific question instead of relying on training data.

# ponytail
- **ponytail** - laziest solution that works: YAGNI, stdlib/native first, minimal diff. Active by default on every coding task. Trigger: "ponytail", "lazy mode", "simplest solution".

# java-engineer
- **java-engineer** - Java 17-25 policy and pitfalls (records, sealed types, switch exhaustiveness, Optional, virtual threads, null-safety). Use when writing, reviewing, or refactoring Java code.
